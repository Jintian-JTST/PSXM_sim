__pycache__
.vscode
test

# ROOT files: the old rule ".root" only matched a file literally named
# ".root", so trk20000.root was never actually ignored.
*.root

# local tooling / personal notes -- not part of the codebase
.claude/
.remember/

# LaTeX build products, if the report is ever moved inside the repo
*.aux
*.log
*.out
*.toc

# report scripts also regenerate these analysis figures into figures/;
# they are not used by the report, which keeps only its seven figures
figures/background_field.png
figures/rot_quad_fields.png
figures/shield_current_map.png
figures/shield_radius_selection.png

# weekly deliverables / large binaries -- kept locally, not versioned
weekly_report_*.pptx
weekly_report_*.html
